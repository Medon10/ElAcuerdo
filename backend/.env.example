# Backend environment example
# Copy to backend/.env and fill with your real values

# --- App ---
NODE_ENV=development
JWT_SECRET=change_me
JWT_EXPIRES_IN=12h
FRONTEND_ORIGINS=http://localhost:5173

# --- Database (MySQL) ---
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=el_acuerdo
DB_TIMEZONE=local

# Alternative: single connection string (recommended for cloud DBs)
# Example:
# DB_URL=mysql://user:pass@host:4000/el_acuerdo
# DB_URL=

# TiDB Cloud usually requires TLS
DB_SSL=true
DB_SSL_REJECT_UNAUTHORIZED=true

# --- Email notifications (SMTP) ---
# Enable/disable sending
MAIL_ENABLED=true

# Who receives the notifications (boss email). For testing you can put your own email here.
MAIL_TO=tu_mail_de_prueba@gmail.com

# From address (often must match SMTP_USER)
MAIL_FROM=tu_mail_de_prueba@gmail.com

# SMTP credentials
# If you use Gmail, use an App Password (not your normal password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=tu_mail_de_prueba@gmail.com
SMTP_PASS=app_password_aqui
